<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Resochat</title>
</head>

<body>
    <% if (update_done == true) { %> <h1>All updates has been successfully done</h1> <% } %>
    <form action="<%= configs.admin.route %>" method="POST">
        <h2>Server Configuration</h2>
        <label for="ip">Server IP:</label>
        <input type="text" id="ip" name="ip" value="<%= configs.server.ip %>"><br><br>

        <label for="port">Server Port:</label>
        <input type="number" id="port" name="port" value="<%= configs.server.port %>"><br><br>

        <h2>Logs Configuration</h2>
        <label for="showLogs">Show Logs:</label>
        <input type="checkbox" id="showLogs" name="showLogs" <% if (configs.logs.show_logs == true) { %> checked <% } %>><br><br>

        <label for="saveLogs">Save Logs:</label>
        <input type="checkbox" id="saveLogs" name="saveLogs" <% if (configs.logs.save_logs == true) { %> checked <% } %>><br><br>

        <h2>Admin Configuration</h2>
        <label for="adminEnabled">Admin Enabled:</label>
        <input type="checkbox" id="adminEnabled" name="adminEnabled" <% if (configs.admin.enabled == true) { %> checked <% } %>><br><br>

        <label for="adminRoute">Admin Route:</label>
        <input type="text" id="adminRoute" name="adminRoute" value="<%= configs.admin.route %>"><br><br>

        <h2>Account Configuration</h1>

        <label for="username">Admin username (leave empty for not changing)</label>
        <input type="text" id="username" name="username" value=""><br><br>
    
        <label for="port">Admin password (leave empty for not changing)</label>
        <input type="password" id="password" name="password" value=""><br><br>

        <input type="submit" value="Submit">
    </form>
</body>

</html>